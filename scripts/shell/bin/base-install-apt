#!/bin/bash

sudo apt update
sudo apt install -y build-essential curl wget git unzip zip tar \
  software-properties-common ca-certificates apt-transport-https \
  gnupg lsb-release vim nano htop net-tools dnsutils
